# Matches backend PORT=3000 and /api/v1 prefix
VITE_BACKEND_API_URL=http://localhost:3000/api/v1
VITE_BACKEND_SOCKET_URL=http://localhost:3000

# OPTIONAL — leave blank for local testing. The Razorpay checkout button
# will not open without this, but nothing else in the app is affected.
VITE_RAZORPAY_KEY_ID=
